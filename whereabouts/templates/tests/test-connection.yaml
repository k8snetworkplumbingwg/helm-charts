apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whereabouts.fullname" . }}-test-connection"
  labels:
    {{- include "whereabouts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whereabouts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
